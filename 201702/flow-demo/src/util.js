/**
 * @flow
 * util
 * @author    : yunchen
 * @createdAt : 09/02/2018
 */
const UTIL_ID = 'uid';

/**
 * 加法运算
 * @param {number} a - 加数a
 * @param {number} b - 加数b
 */
export const add = (a: ?number, b: ?number): ?number => {
    return a + b;
}
